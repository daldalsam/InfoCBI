# 중학교 정보 수업용 웹 앱 모음

## 프로젝트 개요

중학교 정보 교과(2022 개정 교육과정) 수업에 사용할 웹 앱들을 단원별로 개발하는 저장소입니다.
개발자는 중학교 정보 교사이며 개발 경험이 없습니다. 바이브코딩 방식으로 작업합니다.

## 개발자 지원 방식

- 전문 용어를 쓸 때는 짧은 설명을 함께 붙입니다.
- 코드를 작성한 뒤에는 무엇을 왜 그렇게 했는지 초보자 수준으로 요약합니다.
- 터미널 명령이 필요하면 대신 실행하고, 결과를 설명합니다.
- 큰 작업은 한 번에 하지 않고 기능 단위로 쪼개서 매 단계 확인을 받습니다.

## 폴더 구조

교과서(비상교육, 2022 개정) 대단원 5개를 따릅니다. 중단원은 접고 소단원 번호를
이어 붙였습니다. **수업 설계를 하는 단원만** 폴더를 만들고, 필요해지면 추가합니다.
앱이 없는 단원(토론·조사·외부 도구 실습)도 폴더를 만듭니다 — `lesson.md`를 둘
자리가 필요하기 때문입니다. 그 폴더에는 `index.html`만 없습니다.

```
info-class-apps/
├── CLAUDE.md                     최상위 지침 (전체 공통 규칙)
├── index.html                    메인 목차 페이지 (각 앱으로 링크)
├── docs/
│   ├── curriculum.md             교육과정 성취기준 + 앱 아이디어
│   ├── textbook.md               교과서 단원 구성 + 개발 순서
│   ├── pedagogy.md               수업 설계 원칙
│   ├── registry.md               단원 대장 — 축·낱말·동사·봉인·경계 충돌 점검
│   └── lesson-template.md        단원 설계 템플릿
├── teacher/
│   ├── index.html                교사용 첫 페이지 (단원 목록·차시 수·지도안 링크만)
│   └── 02-data/
│       └── 01-digital-data/      단원별 교사 자료 폴더 (단원 폴더와 같은 경로)
│           ├── 01-digital-data.html   지도안 — 설계 내용 + 차시별 상세 지도안
│           └── worksheet-<대단원번호>-<단원>-lessonN.docx
│                                 차시별 학습지 등 수업 자료
├── 01-computing-system/
│   ├── 01-system-structure/
│   └── 03-device-planner/
├── 02-data/
│   ├── 01-digital-data/
│   ├── 02-representation/
│   ├── 03-structuring/
│   └── 04-analysis/
├── 03-algorithm-programming/
│   ├── 01-problem-state/
│   ├── 02-abstraction/
│   ├── 03-expression/
│   ├── 04-analysis/
│   ├── 05-logic/
│   ├── 06-nested-control/
│   ├── 07-list/
│   └── 08-function/
├── 04-ai/
│   ├── 01-ai-concept/
│   └── 02-data-learning/
└── 05-digital-culture/
    ├── 01-privacy/
    └── 02-copyright/
```

각 단원 폴더 안에는 세 파일이 들어갑니다.

```
CLAUDE.md      이 단원 전용 지침 + 진행 기록
lesson.md      이 단원 수업 설계 (앱 개발 전에 작성)
index.html     앱 본체 — 앱이 있는 단원만
```

규칙:

- 폴더 이름은 두 자리 숫자 접두사 + 영문 소문자 이름.
- 앱 하나는 하나의 폴더에 `index.html`로 완결되게 만듭니다.
- **앱이 없는 단원은 `index.html`을 만들지 않습니다.** 학생용 첫 페이지에도
  링크를 걸지 않고, 교사용 첫 페이지에만 「앱 없음」 표시와 함께 올립니다.
  앱이 없다고 해서 `lesson.md`를 생략하지는 않습니다 — 설계는 앱 유무와
  무관하게 이 프로젝트의 원본입니다.
- 폴더는 **교과서 단원 기준**입니다. 성취기준과 일대일로 맞지 않아도 되며,
  대응 관계는 `docs/textbook.md`의 표에 정리돼 있습니다.
- 새 단원 폴더가 필요하면 `docs/textbook.md`의 폴더명을 그대로 씁니다.
  **설계 탭이 혼자 이름을 짓지 않습니다.** `textbook.md`의 폴더 열이 `—`이면
  아직 묶음 방식이 안 정해진 것이니, **총괄 대화에서 정해 그 표에 적은 뒤**
  폴더를 만듭니다(절차는 `textbook.md` 「폴더 이름이 「—」인 단원을 시작할 때」).
- 위 구조도는 **최종 목표 모습**입니다. 아직 설계하지 않은 폴더는 실제로 없습니다.

## 폴더별 CLAUDE.md

**단원 폴더에만** 만듭니다(앱이 없는 단원 폴더도 포함). `docs/` 같은 자료 폴더나
생성물 폴더(`node_modules`, `.git`, `dist` 등)에는 만들지 않습니다.

담을 내용:

| 항목 | 내용 |
|---|---|
| 목적 | 이 단원이 무엇을 다루는가, 관련 성취기준 |
| 구성 | 안에 있는 파일과 각각의 역할 |
| 진행 상황 | 현재 상태, 다음에 할 일 |
| 결정사항 | 이 앱에만 해당하는 설계 결정과 그 이유 |

주의사항:

- **최상위 지침과 겹치는 내용을 반복해서 적지 않습니다.** 이름 짓기 규칙, 기술 스택,
  개인정보 원칙 등은 최상위에만 둡니다. 여러 곳에 흩어지면 규칙을 바꿀 때 반드시
  한두 곳을 놓칩니다.
- 이 폴더에만 해당하는 내용만 적습니다.
- **진행 상황은 "현재 상태 + 다음 할 일" 중심으로 짧게** 유지합니다. 지난 기록이 계속
  쌓이면 파일이 길어져서 정작 개발에 쓸 여유가 줄어듭니다. 오래된 내용은 정리합니다.

## 진행 기록 갱신 시점 — 커밋 전

**커밋하기 전에, 그 작업으로 바뀐 앱 폴더의 `CLAUDE.md`에서 진행 상황과 다음에 할 일을
갱신합니다. 갱신 없이 커밋하지 않습니다. 갱신한 내용은 사용자에게 한 줄로 알립니다.**

갱신할 내용이 없다고 판단하면 그 이유를 한 줄로 말합니다.

**함께 점검할 것 — 새 `lesson.md`가 추가된 커밋이라면:**
그 단원이 `docs/pedagogy.md`의 「단원별 개념 한눈에 보기」 표에 있는지 확인합니다.
없으면 커밋을 멈추고 사용자에게 알립니다. `lesson.md` 안의 「pedagogy.md에 추가할 한 줄」
항목을 그대로 옮겨 적으면 됩니다.

이 표는 홈 탭에서 다음 단원을 설계할 때 이전 맥락을 전달하는 유일한 통로입니다.
빠지면 단원 간 개념 연결이 끊깁니다.

**함께 점검할 것 — `lesson.md`를 고친 커밋이라면:**
그 단원 교사 폴더의 지도안(`<단원명>.html`)도 같이 고쳤는지 확인합니다.
`lesson.md`가 원본이고 지도안은 사본이라, 한쪽만 고치면 어긋납니다.
`teacher/index.html`은 차시 수가 바뀔 때만 고치면 됩니다.

작업이 완결되지 않은 상태에서 커밋해도 괜찮습니다. 미완성 상태를 기록해두는 것이
다음에 이어받을 때 더 도움이 됩니다.

## 작업 시작 시

1. 이 파일을 먼저 읽습니다.
2. 작업할 단원 폴더의 `CLAUDE.md`를 읽고 진행 상황을 확인합니다.
3. 그 폴더의 `lesson.md`(수업 설계)를 읽습니다. **앱은 여기서 "앱이 담당할 것"으로
   정한 범위만 만듭니다.** 설계에 없는 기능을 임의로 추가하지 않습니다.
4. `docs/pedagogy.md`의 수업 설계 원칙을 확인합니다.
   이어서 **`docs/registry.md`(단원 대장)**에서 **축 배분·낱말 배정·활동 동사·
   봉인·경계**를 확인합니다. **단원끼리 부딪히는지 보는 곳은 여기 하나입니다** —
   앞 `lesson.md`들을 훑어 짜맞추지 않습니다.
5. `docs/curriculum.md`에서 해당 단원의 성취기준을 확인합니다.
6. **같은 중단원의 이전 단원 `lesson.md`를 함께 읽습니다.**
   개념적 렌즈는 중단원 단위로 공유합니다. 소단원별 역할 분담(축 배분)은
   **`docs/registry.md` 표 1**에 있으니 거기서 확인하고, 이전 `lesson.md`에서는
   **그 축이 실제로 어떤 조작으로 살아났는지**만 봅니다(`docs/pedagogy.md`의 표로
   넘어가는 것은 렌즈와 일반화 한 줄뿐입니다).
   읽지 않으면 두 단원이 거의 같은 설계가 됩니다.
   예 — `02-abstraction`을 설계할 때는 `01-problem-state/lesson.md`를 먼저 읽습니다.
7. **대단원이 바뀔 때도 앞 `lesson.md`를 통째로 읽어 옮기지 않습니다.**
   **축 배분·낱말 배정·활동 동사·봉인·경계는 `docs/registry.md`에서 확인합니다.
   앞 `lesson.md`는 그 단원의 수업 내용이 필요할 때만 읽고, 대조표는 베끼지
   않습니다.**
   서술의 **형식**(검수표 짜는 법·금지 목록·자를 순서·시점 표기)은 가장 최근
   `lesson.md`에서 봅니다 — **형식만 가져오고 내용은 옮기지 않습니다.** 렌즈를
   이어받거나 뒤집을 대상이 되는 앞 대단원의 `lesson.md`는, 그 판단을 실제로
   근거로 쓸 때 **그 대목만** 읽습니다.
   근거 — 2026-08-21에 `docs/registry.md`가 생긴 뒤 `04-ai/04-ai-ethics/lesson.md`는
   교차 언급 **11회·26KB**가 됐습니다(직전 단원은 89회·93KB). 반대로 베껴 오던
   때에는 **Ⅳ의 `lesson.md` 셋이 Ⅲ-1-01·02의 조작 어휘를 Ⅱ-1-01·02로 잘못 적어**
   오류가 세 단원에 퍼졌습니다(`docs/registry.md` 표 1 아래의 정정).
8. **앱을 만들 때는 가장 최근에 만든 `index.html`도 함께 읽습니다.**
   1~7번은 설계 문서라 **코드 관습이 전달되지 않습니다** — 계산부를
   `[엔진 시작]~[엔진 끝]`으로 격리하는 구조, 320px 처리 방식, 색＋무늬 이중 구분,
   한국어 조사 함수가 최근 앱에만 있습니다. 옛 앱에서 복사하면 고쳐둔 버그가
   되살아납니다(아래 「기술 스택」의 조사 함수 항목).

**차시를 정할 때는 `docs/textbook.md`의 「연간 시수 예산」을 먼저 봅니다.**
차시는 단원마다 따로 정하는 값이 아니라 **60차시를 나눠 쓰는 값**입니다.
기본값은 소단원당 2차시이고, 넘기려면 그 표에서 **어디를 줄일지 함께 정합니다.**
정하지 않고 늘리면 뒤 영역이 통째로 밀립니다. 확정된 차시는 그 표의
「확정된 차시」 항목에 옮겨 적습니다.

`lesson.md`가 없으면 앱 개발을 시작하지 말고 사용자에게 알립니다.
수업 설계는 설계 탭에서 먼저 하기로 정했습니다. 설계가 구현에 끌려가지 않게 하기
위한 순서입니다.

**설계 대화에서는 앱 코드를 만들지 않습니다.** 설계 탭에서 코드가 나오기 시작하면
무게중심이 구현으로 넘어가고, 교육적 의도가 구현 편의에 맞춰 깎입니다.
설계 결과는 `lesson.md`로만 남깁니다.

## 작업 종료 시

1. 바뀐 앱 폴더의 `CLAUDE.md` 진행 상황을 갱신합니다.
2. 커밋하고 푸시합니다.

세션 기록은 컴퓨터마다 따로 저장되므로 남지 않습니다. 맥북과 데스크톱을 번갈아 사용하기
때문에, 이어가야 할 맥락은 반드시 파일에 기록합니다.

## 이름 짓기 규칙

- 폴더와 파일 이름은 **영문 소문자와 하이픈만** 사용합니다.
- 한글은 앱 화면에 표시되는 텍스트에만 씁니다.
- 이유: 한글 경로는 웹 주소에서 깨져 보이고, 맥과 윈도우의 한글 처리 방식이 달라 기기를
  오갈 때 문제가 생깁니다.

## 첫 페이지 구성 — 학생용과 교사용을 나눕니다

```
index.html            학생용. 단원 앱 링크만. 큼직하고 짧게
teacher/index.html    교사용 첫 페이지. 단원 목록·차시 수·지도안 링크만
teacher/<단원 경로>/<단원명>.html
                      단원별 설계 내용(일반화·탐구 질문·앱 설명·수업 흐름·
                      과제 질문)과 차시별 상세 지도안. 설계 내용은 여기에만
```

교사 자료는 **단원 폴더**(`teacher/` + 단원 폴더 경로)에 모읍니다. 지도안은
`<단원명>.html`(예: `teacher/02-data/01-digital-data/01-digital-data.html`),
그 옆에 학습지(docx) 같은 수업 자료를 함께 둡니다. 학습지 이름은
**`worksheet-<대단원번호>-<단원>-lessonN.docx`**로 대단원 번호와 차시를 붙이고
(예: `worksheet-02-01-digital-data-lesson1.docx`), 클래스룸 과제 제목도
`[데이터 01-1차시] …`처럼 차시를 넣습니다 — 한 단원에 과제가 여러 개
쌓이면 제목만으로 구분되지 않습니다.

**학습지에만 대단원 번호를 붙이는 이유:** 소단원 이름은 대단원이 달라도 같아질 수
있습니다(예: `02-data/04-analysis`와 `03-algorithm-programming/04-analysis`).
지도안 `.html`은 링크로 열리므로 폴더 경로가 둘을 갈라주지만, **학습지 docx는
내려받는 순간 폴더 맥락이 사라져** 교사의 다운로드 폴더에서 같은 이름끼리
부딪칩니다. 그래서 지도안에는 접두사를 붙이지 않고 학습지에만 붙입니다.
클래스룸 과제 제목은 `[데이터 04-…]`/`[알고리즘 04-…]`로 이미 갈리므로
그대로 둡니다. 클래스룸 과제 질문은
`lesson.md`(원본)와 지도안에 이미 있으므로 **별도 파일로 만들지 않습니다** —
사본이 늘면 고칠 때 반드시 한 곳을 놓칩니다.
`index.html`이라는 이름은 첫 페이지 둘과 앱에만 씁니다.
그래서 교사용 단원 폴더 주소를 직접 열면 404가 나옵니다 — 지도안은 교사용
첫 페이지에서 들어갑니다.

설계 내용을 `teacher/index.html`에 두지 않는 이유: 사본이 세 겹(lesson.md →
첫 페이지 → 지도안)이면 단원이 늘어날 때 반드시 어긋납니다. 두 겹
(`lesson.md` → 지도안)으로 유지합니다.

**루트는 반드시 학생용입니다.** 학생은 링크 앞부분을 자르거나 주소창 자동완성으로
루트에 도착합니다. 거기에 교사용 페이지가 있으면 「일반화 문장」과 「학생이 발견할
것」이 먼저 보여서, 그 뒤의 탐구 활동이 확인 문제가 됩니다.

교사용 페이지는 같은 교과 선생님과 공유할 수 있는 수준으로 만듭니다. 다만 무료
요금제에는 비밀번호 기능이 없어 **주소를 아는 사람은 누구나 봅니다.** 넣을 내용을
정할 때 이걸 전제로 판단합니다.

**결정 — 교사용을 별도 사이트로 분리하지 않습니다.** 같은 저장소로 넷리파이
사이트를 하나 더 만들고 base directory를 `teacher/`로 두면 교사용이 완전히 다른
주소를 갖게 되지만, 그렇게 하지 않기로 했습니다. 학생에게는 **개별 앱 링크만
구글 클래스룸으로** 주므로 학생이 `/teacher/`를 떠올릴 일이 사실상 없고, 주소를
잘라 루트로 가더라도 거기는 학생용이기 때문입니다. 관리할 배포 설정도 하나로
유지됩니다. 이 결정은 다시 논의하지 않습니다.

대신 지킬 것 — **학교 공용 태블릿에서는 교사용 페이지를 열지 않습니다.**
브라우저 주소창 자동완성에 `/teacher/`가 남아 다음 사용자에게 노출됩니다.
교사용은 개인 기기에서만 엽니다.

`lesson.md`가 원본이고 단원별 지도안 페이지는 옮겨 적은 사본입니다.
한쪽만 고치면 어긋납니다.

## 링크 규칙

- 앱 간 링크는 항상 **상대 경로**로 작성합니다. (`02-data/01-digital-data/index.html`)
- 맨 앞에 `/`를 붙이지 않습니다. 로컬에서 열 때 깨집니다.

## 기술 스택

**이 항목은 「자작 앱을 어떻게 만드는가」입니다.** 수업에서 쓰는 외부 도구
(코답·엔트리·티처블 머신·마이크로비트 편집기)와는 무관합니다 — 그쪽 기준은
`docs/pedagogy.md` 「작업 도구 등록표」에 따로 있습니다.

- 자작 앱은 HTML, CSS, JavaScript만 사용합니다. 프레임워크나 빌드 도구는 쓰지 않습니다.
- 외부 라이브러리는 꼭 필요한 경우에만, CDN으로 불러옵니다.
- **브라우저에서 컴파일하는 방식은 쓰지 않습니다.** (Babel standalone,
  Tailwind CDN 등) 학생 기기에서 페이지를 열 때마다 변환이 일어나 매우 느려집니다.
- 큰 이미지나 동영상을 앱에 직접 넣지 않습니다. 영상은 유튜브 링크로 불러옵니다.
- **한국어 조사(이/가·은/는·을/를·으로/로)를 코드로 붙일 때는 가장 최근 앱의 조사
  함수를 가져옵니다.** `03-algorithm-programming/04-analysis`에서 「으로/로」의
  ㄹ받침 예외(퇴근길**로** ○ / 퇴근길**으로** ✗)를 고쳤고, **그보다 앞선 앱들에는 그
  버그가 남아 있습니다.** 옛 앱에서 복사하면 버그를 되살립니다.
- 이유: 학교 네트워크와 저사양 기기에서도 빠르게 열려야 하고, 개발자가 유지보수할 수
  있는 수준을 유지해야 합니다.

## 개인정보 원칙

- **학생의 이름, 학번, 성적 등 개인정보를 저장하는 기능은 만들지 않습니다.**
- 브라우저를 닫으면 사라지는 방식으로 만듭니다. 서버나 데이터베이스를 쓰지 않습니다.
- 기록이 남아야 하는 활동(발견 기록, 성찰, 제출물)은 앱이 아니라 구글 클래스룸에서
  처리합니다. 자세한 역할 분담은 `docs/pedagogy.md` 참고.

## 저작권 주의

- 교육과정 성취기준은 공공 문서이므로 저장소에 포함해도 됩니다.
- `docs/textbook.md`에는 **목차 정보만** 담습니다. 교과서 본문 내용은 출판사
  저작물이므로 정리해 넣지 않습니다. 특정 단원의 서술 방식을 참고해야 하면 그
  단원의 `lesson.md`에 몇 줄만 요약합니다.
- 앱에 넣는 이미지나 자료는 직접 만든 것, 또는 라이선스가 확인된 것만 사용합니다.
- **`lesson.md`의 교과서 서술 요약은 `teacher/` 폴더의 어떤 페이지로도 옮기지
  않습니다.** 교사용 페이지들은 공개 배포물이고, 교과서 본문은 출판사 저작물입니다.
  성취기준은 공공 문서이므로 옮겨도 됩니다.
- **교과서 쪽수도 `teacher/` 폴더의 어떤 페이지에도 적지 않습니다.** 쪽수는
  `lesson.md`에만 둡니다. (차시 수는 적어도 됩니다.)
- **배포물은 저장소의 모든 파일을 웹에 노출합니다.** 저장소가 비공개인 것과 무관합니다.
  `netlify.toml`의 배포 명령이 `.md` 파일과 `.claude` 폴더를 배포물에서 제외합니다
  (넷리파이 주소 규칙은 확장자 패턴을 지원하지 않아 404 돌리기 대신 제외 방식).
  배포 후 `.md` 주소를 직접 열어 404가 나오는지 확인합니다.
- **교사 자료 중 학습지 docx 등 `.md`가 아닌 파일은 공개로 배포됩니다.**
  주소를 알면 누구나 내려받을 수 있으므로, 교과서 내용이 없는 자작 자료만
  둡니다.

## 실행과 배포

- 로컬 확인은 미리보기 서버(localhost)로 합니다. 파일을 더블클릭하는 방식은 자바스크립트
  기능이 있으면 작동하지 않습니다.
- 저장소는 비공개로 두고, 넷리파이(Netlify)로 배포합니다.
- 푸시하면 자동으로 배포가 갱신됩니다.

**배포 주소** — `https://daldalsam-info.netlify.app`

| 용도 | 주소 |
|---|---|
| 학생용 첫 페이지 | `/` |
| 앱 — 점 몇 개면 될까? | `/02-data/01-digital-data/` |
| 앱 — 그림일까, 글자일까, 수일까? | `/02-data/02-representation/` |
| 교사용 (동료 공유용) | `/teacher/` |

학생에게는 **앱 주소만** 클래스룸으로 안내합니다. 사이트 이름을 다시 바꾸면
이미 나눠준 링크가 모두 죽으므로 바꾸지 않습니다.

## 환경

- 맥북과 윈도우 데스크톱을 번갈아 사용합니다.
- 깃허브 인증은 `gh`(깃허브 CLI)로 합니다. 맥북은 인증 완료. **새 기기에서는
  `gh auth login`을 한 번 실행**해야 푸시가 됩니다.
- 줄바꿈 문자 통일을 위해 `.gitattributes`에 `* text=auto eol=lf`를 유지합니다.
- 파일 이름의 대소문자를 항상 일치시킵니다. (로컬에서는 통과하지만 배포 서버에서 깨집니다.)

## 남은 작업

### 완료

- [x] `docs/curriculum.md` — 교육과정 성취기준 + 앱 아이디어
- [x] `docs/textbook.md` — 교과서 단원 구성 + 개발 순서
- [x] `docs/pedagogy.md` — 수업 설계 원칙
- [x] `docs/lesson-template.md` — 단원 설계 템플릿
- [x] 폴더 구조 확정
- [x] 전역지침(`~/.claude/CLAUDE.md`)에서 "폴더별 CLAUDE.md 작성" 항목 삭제
- [x] `git init`, `.gitignore`, `.gitattributes`, 첫 커밋
- [x] 깃허브 연결 및 첫 푸시 — `daldalsam/info-class-apps` (비공개)
- [x] **Ⅱ-1-01 디지털 형태의 데이터** — `lesson.md` + 앱(샘플링 조작기)
      + 지도안 + 학습지 2종 (2차시)
- [x] **Ⅱ-1-02 다양한 데이터의 디지털 표현** — `lesson.md` + 앱(비트 해석 전환기)
      + 지도안 + 학습지 3종 (3차시)
- [x] 최상위 `index.html` — 학생용 목차. 카드 하나 복사해 붙이는 식으로 앱 추가
- [x] `teacher/index.html` — 교사용 첫 페이지. 단원 목록·차시 수·지도안 링크
      (설계 내용은 단원별 지도안 페이지에)
- [x] `netlify.toml` — 배포 시 `.md`와 `.claude`를 제외 (직접 열면 404)
- [x] **넷리파이 연결 및 배포** — `daldalsam-info.netlify.app`.
      `.md` 7개와 `.claude`가 실제로 404인 것까지 확인 완료
- [x] 학습지 5종 개명 — `worksheet-<대단원번호>-<단원>-lessonN.docx` 규칙 적용,
      지도안 2종의 다운로드 링크도 함께
- [x] **학생 기기에서 실제 확인** — 태블릿·크롬북에서 `02-representation`
      앱의 탭·드래그 동작 확인 완료
- [x] 교사용 페이지 분리 여부 결정 — 분리하지 않기로 확정
      (근거는 「첫 페이지 구성」 항목에)
- [x] **Ⅲ-1-01 문제의 상태와 구조화** — `lesson.md` + 앱(강 건너기 + 정보
      카드 9장) + 지도안 + 학습지 2종 (2차시). 학생 기기 확인과
      수업 후 기록만 남음

### 남은 것 (이 순서대로)

1. [ ] **Ⅲ-1 수업 실행** — 네 소단원의 제작(설계·앱·지도안·학습지)은 모두
       끝났습니다. 남은 것은 클래스룸 게시(앞 소단원을 마친 반에만)·학생 기기
       확인·`lesson.md` 8번 수업 후 기록. 소단원별 상세는 각 폴더 `CLAUDE.md`.
       Ⅲ-1에서 확정된 것 셋 — 렌즈 「추상화」, 02와 03은 따로 유지(성취기준
       9정03-02를 나눠 맡음), 소단원당 2차시. 근거와 축 배분표는
       `01-problem-state/lesson.md` 1번·4번
2. [ ] **Ⅳ-1 인공지능과 학습 — 두 소단원 제작 완료, 총괄 안건 둘 남음**
       Ⅳ-1에서 확정된 것 셋 — 렌즈 **「학습」**(「표현」은 보조 개념으로 내림),
       소단원당 2차시(Ⅳ-1 합 4차시), Ⅳ-2는 소단원 4개를 2개 설계로 묶고 같은
       렌즈 공유 — **Ⅳ-2는 6차시**(실습 묶음 4 + 윤리 2)로 잡아 **대단원 Ⅳ 합
       10차시**. 근거는 `docs/textbook.md` 「연간 시수 예산」.
       근거와 축 배분표는 `04-ai/01-ai-concept/lesson.md` 1번.
       - [x] **Ⅳ-1-01 인공지능의 개념과 특성 — 제작 완료(2026-08-18).**
             `lesson.md` + 앱 「어떻게 가려낼까?」(검수 16항 통과, 3차 검증
             반영한 4차 수정까지) + 지도안 + 학습지 1종 + 첫 페이지 두 줄
             (카드 「스팸 거르기」). 남은 것은 클래스룸 게시(내년 진도 도달
             시)·학생 기기 확인·수업 후 기록
       - [x] **Ⅳ-1-02 데이터와 학습 — 제작 완료(2026-08-19).**
             `lesson.md` + 앱 「무엇을 보여 줄까?」(하네스 33항 통과, 2차 검증
             반영까지) + 지도안 + 기록지 1종 + 첫 페이지 두 줄
             (카드 「분리배출 도우미」). 축은 「뽑아낸 규칙이 무엇을
             물려받는가」(편향), 소재는 **학교 분리배출**, 2차시로 예산표 수정
             없음. 검수표 기대값은 전수 계산으로 확정(예산 6장 1056가지·해제
             41471가지). 남은 것은 클래스룸 게시(**01의 2차시를 마친 반에만**)·
             학생 기기 확인·수업 후 기록
       - [x] **총괄 안건 해소 — 티처블 머신 등록 완료(2026-08-19 중간점검).**
             「승인된 예외」 표가 **「작업 도구 등록표」로 개편**되었고, 티처블
             머신·엔트리(또는 스크래치)·마이크로비트 편집기가 함께 등록됐습니다.
             Ⅳ-2 설계에 바로 들어갈 수 있습니다. 폴더명도 확정 —
             `04-ai/03-ai-practice`(4차시) / `04-ai/04-ai-ethics`(2차시)
       - [ ] **Ⅳ-2 인공지능과 문제 해결 — 두 설계 모두 자작 앱을 만들지 않습니다**
             (2026-08-19 총괄 결정). 설계 탭에 앱 아이디어를 제안하지 말라고
             명시할 것. 근거 셋:
             ① 조작과 즉시 반응이 **티처블 머신 안에 이미 있습니다.**
                `docs/pedagogy.md`의 앱 적합도 기준("없으면 앱으로 만들지 않는다")에
                따라 만들 이유가 없습니다
             ② **같은 렌즈를 세 번째로 조작하게 됩니다.** Ⅳ-1의 앱 둘
                (「어떻게 가려낼까?」 = 규칙 판 vs 예시 판 / 「무엇을 보여 줄까?」 =
                편향)이 렌즈 「학습」을 이미 두 번 다뤘습니다. 세 번째는
                깊어지지 않습니다. **이 단원이 앞 둘과 다른 점은 개념이 아니라
                「진짜로 해본다」는 것**입니다
             ③ `docs/curriculum.md`의 *"자체 앱은 학습 과정을 단순화해 **보여주는**
                정도"* 아이디어는 방식 A의 *"애니메이션으로 원리를 대신 보여주지
                않습니다"*에 걸립니다 — 방식 A를 확정하기 전에 적힌 것입니다
             `04-ai-ethics`도 같습니다 — 토론이 중심이라 앱으로 대체하면 취지가
             훼손됩니다. **티처블 머신이 막히거나 기기가 부족할 때의 대안도
             자작 앱이 아니라** 소단원 선택(텍스트는 웹캠·마이크 불필요)·교사
             시연·모둠 1대로 풉니다.
             **개인정보 — 소단원 01은 학생 신체가, 02는 목소리가 수집됩니다.**
             셋 중 둘을 고를 때 이 부담을 반드시 따집니다(03 텍스트가 가장 적음)
       - [x] **Ⅳ-2 실습 묶음 — `lesson.md` 설계 완료(2026-08-20).**
             `04-ai/03-ai-practice/lesson.md`(4차시). **자작 앱 없음** —
             `index.html`을 만들지 않고 학생용 첫 페이지에도 카드를 두지
             않습니다. 확정 — **소단원 01(팔 이미지) 하나**를 고르고 그 안에서
             **데이터 종류를 둘로**(사진＝픽셀 / 모션＝관절). 03 텍스트는
             **티처블 머신에 텍스트 프로젝트가 없어** 탈락(도구 추가는 총괄
             안건). 축은 「**딸려 들어간 것 — 특히 나 자신**」, 「정확도로 재는
             것의 한계」는 재지 않음(Ⅲ-1-04·Ⅳ-1-02가 이미 함), 패들렛은 4차시만.
             **개인정보는 회피하지 않되 동의서도 만들지 않습니다** — 소재를
             교과서 그대로 학생의 몸으로 두고, 수업·학교 활동 동의는 **학기 초
             일괄 동의로 갈음**합니다(소재를 사물로 갈아탄 초안은 축의 가장 강한
             사례를 잃어 폐기).
             **소리(02)는 탈락 — 실제 수업에서 학생 간 간섭으로 진행이
             불편했습니다.** 대신 **소단원 01 하나 안에서 데이터 종류를 둘로**
             갈랐습니다 — **사진(픽셀) ＋ 모션(관절)**. 모션 모델은 배경·옷을 덜
             배우지만 **팔 길이·앉은 키는 여전히 배우므로**, 딸려 들어가는 것이
             없어지는 게 아니라 **바뀝니다** — 도착 문장이 「무엇을 보게 할지
             정하는 것도 사람이다」입니다. **가정통신문·동의서는 만들지 않습니다**
             (학기 초 일괄 동의로 갈음 — 2026-08-20 확정).
             **교사 자료 제작 완료(2026-08-20)** — 지도안
             `teacher/04-ai/03-ai-practice/03-ai-practice.html`(115KB, 차시별 진행을
             누적 시각으로 적어 45×4 검산 표시) ＋ 기록지 2종
             (`worksheet-04-03-ai-practice-lesson1.docx` 1·2차시 3면,
             `worksheet-04-03-ai-practice-lesson3.docx` 3차시) ＋
             `teacher/index.html` 줄 하나(앱 없음·4차시). **설계 탭 전수 검증
             통과** — 순회 발문 다섯·과제 질문 넷·선택 과제가 `lesson.md`와 글자까지
             같고, 차시별 분 배분 네 개가 모두 45, 교과서 쪽수 0회, 확신도(%) 칸
             없음, 「모션이 더 좋다」 8회는 전부 금지·경계 문맥.
             **남은 것은 수업 2주 전 사전 확인 14항 — 10번을 가장 먼저**
             (모션 모델이 팔 길이 다른 사람에게 흔들리는가 — 3차시 전체가 걸림).
             그다음은 클래스룸 게시(**Ⅳ-1-02의 2차시를 마친 반에만**)·수업 실행·
             `lesson.md` 8번 수업 후 기록
       - [ ] **총괄 안건 — 클래스룸 과제 제목에 소단원 번호를 넣을지.**
             지금 규칙(`[소재 대단원번호-N차시]`)은 한 대단원에 소단원이 늘면
             충돌합니다. Ⅳ-1-02는 2차시 제목을 「[분리배출 04-2차시]」로 두어
             01의 「[인공지능 04-2차시]」와 겹치는 것을 피했지만, 소재 이름이
             겹치는 단원이 나오면 다시 막힙니다

3. [ ] **Ⅱ-2 데이터 분석 — 앱 없이 코답으로**
       재편안이 `docs/textbook.md` 「Ⅱ-2 재편」에 정리돼 있습니다. **설계 탭은
       그 항목을 먼저 읽을 것.** 요지만 옮기면:
       - 소단원 4개 → **두 설계**. `03-structuring`(소단원 01+02, 개념, **4차시**) /
         `04-analysis`(소단원 03·04 중 **하나만**, 프로젝트, **4차시**)
       - 폴더명이 바뀌었습니다 — 옛 `03-collection`/`04-structuring`은 쓰지 않습니다
       - 프로젝트는 **급식 잔반** 권장(실제 수치를 구할 수 있고 학생이 이해관계가 있음)
       - 렌즈 후보는 **「구조화」**. Ⅱ-1의 「표현」에서 심화
       - **코답은 `03-structuring` 2차시부터.** [9정02-03]이 "다양한 형태로
         구조화"를 요구해 도구가 필요합니다. **「사용법 차시」를 따로 만들지 않고
         첫 조작이 곧 도구 학습**이 되게 설계합니다
       - `docs/curriculum.md`의 적합도(○)는 그대로 두기로 함 — 설계 대화 첫
         메시지에 "앱 없이 코답"을 명시해 상쇄
4. [ ] **Ⅲ-2 프로그래밍 — 방식 B(실습·연습)로 진행** ← 개념기반탐구 아님
       논리 연산·중첩 제어 구조·리스트·함수와 디버깅. **렌즈와 일반화를 잡지
       않습니다.** 이유와 문서 처리 방법은 `docs/pedagogy.md` 「방식 B」 참고.
       설계 탭 첫 메시지에 **"이 단원은 방식 B다"를 반드시 밝힐 것** — 안 그러면
       습관대로 렌즈부터 잡으려 듭니다
5. [ ] 두 페이지 다듬기 — 앱이 서너 개 쌓인 뒤에 분류와 배치를 정리

**다음 대상을 Ⅳ-1로 잡은 이유(2026-08 총괄 결정):** 진도 순서로는 Ⅲ-2가
다음이지만, `docs/textbook.md`의 **◎ 표시는 "조작형 앱이 잘 맞는다"는 뜻이지
"개념기반이 잘 맞는다"는 뜻이 아닙니다.** 두 기준이 처음 갈리는 지점이 Ⅲ-2이고,
거기는 방식 B로 갑니다. 개념기반 적합도로 보면 Ⅳ-1-02 「데이터와 학습」이
가장 강합니다 — *"학습한 시스템의 판단은 넣은 데이터의 성질을 물려받는다"* 는
다른 교과에도 통하고, 학습 데이터를 고르면 결과가 즉시 달라져 탐구 구조가
억지 없이 나오며, 논쟁적 질문("편향 없는 데이터가 가능한가")이 진짜로
논쟁적입니다. Ⅳ는 내년 진도라 올해 수업에는 못 쓰지만, 교육과정에 *"내용
영역의 배열순서가 교수·학습의 순서를 의미하지 않으며 재구성할 수 있다"* 고
명시돼 있습니다.

세션을 시작할 때 이 목록을 확인하고, 완료된 항목은 체크 표시로 갱신합니다.
