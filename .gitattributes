# 줄바꿈 문자를 저장소에서 LF로 통일합니다.
# 맥과 윈도우를 오갈 때 파일 전체가 바뀐 것처럼 보이는 문제를 막습니다.
* text=auto eol=lf

# 아래는 텍스트가 아니므로 줄바꿈 변환을 하지 않습니다.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.mp3 binary
*.mp4 binary
*.woff binary
*.woff2 binary
*.pdf binary
